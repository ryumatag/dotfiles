# Point zsh to XDG config directory.
export ZDOTDIR="$HOME/.config/zsh"

# If using a login shell, ensure we still have a usable PATH early.
# (Keep this minimal; full PATH is configured in pathrc later.)
